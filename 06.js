const liczba = 22
const parzystość = liczba % 2 === 0 ? 'Parysta' : 'Nieparzysta'
console.log(parzystość);
const IsLoggedIn = true
const text =  IsLoggedIn ? 'witaj' : 'zaloguj się'
console.log(text)
const liczba2 = -9
const Zagnieżdżenie = liczba2 === 0 ? 'zero' : liczba2 > 0 ? 'parzysta' : 'nieparzysta'
console.log(Zagnieżdżenie)